### 多模块存储器

在主存设计中，将多个完全相同的存储器合并，交替或者并行工作。可以提高存储器的工作效率（提高吞吐量）。

怎么做？使用**低位交叉编址**，提高读取速度。

	低位交叉编址：高位作为模块内地址，低位作为模块地址。这样连续的数据被存放在不同的模块内。

当总线耗时比存取耗时少（总线传输一个数据的时间 < 主存模块完成一次读取的时间） 可以利用低位交叉编址读取数据提高吞吐。
#### 轮流启动方式

#### 同时启动方式