
```
socket()
创建一个 TCP socket 对象

bind()
给这个 socket 绑定本地 IP 和端口

listen()
让这个 socket 进入监听状态

accept()
从这个监听 socket 上接收客户端连接
```

```c
setsockopt(listen_fd, SOL_SOCKET, SO_REUSEADDR, &opt, sizeof(opt));

//函数原型：
int setsockopt(
    int sockfd,
    int level,
    int optname,
    const void *optval,
    socklen_t optlen
);

```

设置IPv4地址结构
```c
struct sockaddr_in addr;  // 用来描述 IPv4 地址和端口的数据结构。

memset(&addr, 0, sizeof(addr));
addr.sin_family = AF_INET;
addr.sin_addr.s_addr = INADDR_ANY;
addr.sin_port = htons((uint16_t)port);
```

# 5. 判断 TCP 连接，最好用什么工具？

最直接的是 `nc`，也叫 netcat。
```
nc -vz 服务器IP 8080
```