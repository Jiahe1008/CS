
通过VLAN 将大型局域网划分成与地理位置无关的逻辑局域网。每个VLAN构成独立且较小广播域。

**VLAN划分方式**
1. 基于端口
2. 基于MAC地址
3. 基于IP地址

**IEEE 802.1Q**

在标准以太网帧插入4B的VLAN标签。位置：==源地址与类型字段中间==
	VLAN前两个字节固定0x8100，表示这是802.1Q帧，后两个字节为VID，唯一标识符。
数据字段最小长度由46B减小到42B，最大长度仍然为1500B

最大帧长由1518B增加到1522B

